# 📱 台灣即時監控平台 - 手機響應式設計指南

## 📋 概述

本平台已針對所有裝置進行完整的響應式設計優化，確保在手機、平板和桌機上都能提供最佳使用體驗。

---

## 🎯 設計目標

✅ **iOS 和 Android 相容**  
✅ **快速載入和流暢操作**  
✅ **易於導航和使用**  
✅ **無障礙設計（WCAG 2.1）**  
✅ **不同網路速度下的最佳化**  

---

## 📱 裝置斷點

### 1. **超小螢幕手機** (≤320px)
- 極端螢幕寬度
- 隱藏副標題和非必要文字
- 單欄佈局
- 最小化間距

### 2. **小型手機** (321-374px)
- 典型的小型手機
- 主要斷點 480px

### 3. **標準手機** (375-599px)
- 主要斷點 768px
- 大部分手機最佳化集中於此
- iOS 標準尺寸

### 4. **平板裝置** (600-959px)
- 次要斷點 600px-959px
- 2 欄或 3 欄佈局

### 5. **桌機** (960px+)
- 全功能顯示
- 3-4 欄佈局

---

## 🔧 主要手機優化

### 核心優化 (768px 以下)

#### 1. **按鈕和觸擊目標**
```css
button, .btn, .back-btn {
    min-height: 44px;     /* iOS 建議 */
    padding: 10px 15px;
    border-radius: 6px;
}
```

#### 2. **表單輸入**
```css
input, textarea, select {
    font-size: 16px;      /* 防止自動放大 */
    padding: 12px;
    width: 100%;
}
```

#### 3. **網格系統**
```css
.grid, .cameras-grid, .features-grid {
    grid-template-columns: 1fr;  /* 單欄 */
    gap: 15px;
}
```

#### 4. **表格優化**
```css
table {
    font-size: 0.85rem;
}
td, th {
    padding: 8px 5px;
}
```

#### 5. **圖片響應**
```css
.camera-image {
    height: 150px;
}
img {
    max-width: 100%;
    height: auto;
}
```

### 超小螢幕優化 (480px 以下)

- 隱藏所有裝飾性文字
- 單一統計卡片（不是 2-3 欄）
- 最小化間距
- 隱藏側邊欄

```css
@media (max-width: 480px) {
    .header p { display: none; }
    .stats-panel { grid-template-columns: 1fr; }
    .sidebar { display: none; }
}
```

### 平板優化 (600-959px)

- 2 欄監視器網格
- 3 欄統計面板
- 保留大部分功能

```css
@media (min-width: 600px) and (max-width: 959px) {
    .cameras-grid { grid-template-columns: repeat(2, 1fr); }
    .stats-panel { grid-template-columns: repeat(3, 1fr); }
}
```

---

## 🎨 設計系統

### 排版
- **標題**: 16px 基礎字體，縮放 1.2-1.5 倍
- **內文**: 基礎 16px（防止 iOS 自動放大）
- **小字**: 0.75-0.85rem

### 間距
- **手機**: 10-15px 內邊距，15px 間隙
- **平板**: 15-20px 內邊距，20px 間隙
- **桌機**: 20px+ 內邊距，20-30px 間隙

### 顏色
- 符合 WCAG AA 對比度標準
- 深色模式支援
- 色盲友善

### 動畫
- 遵守 `prefers-reduced-motion` 設定
- 觸控裝置上禁用懸停效果

---

## 📐 佈局模式

### 1. **單欄佈局**（手機默認）
```
┌─────────────────┐
│   標題          │
├─────────────────┤
│   卡片 1        │
├─────────────────┤
│   卡片 2        │
├─────────────────┤
│   卡片 3        │
└─────────────────┘
```

### 2. **雙欄佈局**（平板）
```
┌──────────────┬──────────────┐
│   卡片 1     │   卡片 2     │
├──────────────┼──────────────┤
│   卡片 3     │   卡片 4     │
└──────────────┴──────────────┘
```

### 3. **三欄佈局**（桌機）
```
┌─────────┬─────────┬─────────┐
│ 卡片 1  │ 卡片 2  │ 卡片 3  │
├─────────┼─────────┼─────────┤
│ 卡片 4  │ 卡片 5  │ 卡片 6  │
└─────────┴─────────┴─────────┘
```

---

## 🔐 安全區域 (瀏海屏手機)

```css
@supports (padding: max(0px)) {
    body {
        padding-left: max(12px, env(safe-area-inset-left));
        padding-right: max(12px, env(safe-area-inset-right));
    }
    .header { padding-top: max(12px, env(safe-area-inset-top)); }
}
```

支援：
- iPhone X/11/12/13/14/15 系列
- Android 瀏海屏設備

---

## 🎭 深色模式

所有頁面都支援深色模式，自動適配系統設定：

```css
@media (max-width: 768px) {
    .dark-mode .card { background: #2a2a2a; }
    .dark-mode input { background: #333; color: #fff; }
}
```

---

## ♿ 無障礙設計

### 鍵盤導航
- 所有互動元素都可透過 Tab 鍵訪問
- 焦點指示清晰可見

### 螢幕閱讀器
- 語義化 HTML 標籤
- ARIA 標籤和角色
- 替代文字（alt text）

### 色彩對比
- 文本對比度 ≥ 4.5:1
- 符合 WCAG AA 標準

### 運動敏感性
```css
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
    }
}
```

---

## 🚀 性能優化

### 圖片優化
- 延遲載入（Lazy Loading）
- 響應式圖片（srcset）
- 圖片壓縮

### CSS 優化
- 單一 mobile-responsive.css 檔案
- 共享類別和 mixin
- 媒體查詢分組

### 字體優化
- 使用系統字體作為後備
- 減少字體檔案下載

### 載入性能
- 首屏時間 < 2 秒（3G）
- 互動時間 < 3 秒

---

## 📋 每個頁面的手機特性

### 交通頁面 (highway.html, expressway.html, road.html, city.html)
✅ 監視器卡片單欄顯示  
✅ 地圖全寬顯示  
✅ 過濾器堆疊  
✅ 統計面板 2 欄（480px 以下為 1 欄）  

### 運輸頁面 (train-liveboard.html, metro-liveboard.html)
✅ 表格橫向滾動（需要時）  
✅ 狀態標籤右對齐  
✅ 車站選擇器下拉菜單  
✅ 實時更新指示器  

### 環境頁面 (air-quality.html, rainfall-radar.html, weather.html)
✅ 圖表響應式縮放  
✅ 資訊卡片單欄  
✅ 雷達地圖全寬  
✅ 數據表格橫向滾動  

### 水資源頁面 (water-resources.html, water-cctv.html)
✅ 儀表板卡片網格 1-2 欄  
✅ 數據表格橫向滾動  
✅ 監視器單欄  
✅ 圖表自動縮放  

---

## 🛠️ 開發指南

### 添加新元素時

1. **確保觸擊目標至少 44px × 44px**
   ```css
   button { min-height: 44px; }
   ```

2. **使用 rem 單位用於調整大小**
   ```css
   .card { padding: 1rem; }  /* 相對於根字體大小 */
   ```

3. **在表單中使用 16px 字體**
   ```css
   input { font-size: 16px; }  /* 防止 iOS 自動放大 */
   ```

4. **測試所有三個主要斷點**
   - 480px（小手機）
   - 768px（大手機/平板）
   - 1200px+（桌機）

5. **考慮橫屏模式**
   ```css
   @media (max-height: 500px) and (orientation: landscape) {
       /* 橫屏優化 */
   }
   ```

---

## 🧪 測試清單

### 手動測試
- [ ] 在 iPhone 6/7/8 上測試
- [ ] 在 iPhone 12 Pro Max 上測試
- [ ] 在 Samsung Galaxy S10 上測試
- [ ] 在 iPad 上測試
- [ ] 在橫屏模式下測試
- [ ] 在 4G/3G 網路下測試
- [ ] 測試所有過濾器和搜尋功能
- [ ] 測試表單提交
- [ ] 測試深色模式
- [ ] 測試無障礙功能

### 自動化測試
```bash
# 使用 lighthouse 檢查
lighthouse https://your-domain.com --mobile

# 檢查響應式設計
# 使用 Chrome DevTools > Ctrl+Shift+M
```

### 瀏覽器相容性
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Samsung Internet 14+

---

## 📊 檔案結構

```
Road-Camera/
├── assets/
│   ├── dark-mode.css           # 深色模式
│   ├── mobile-responsive.css   # 手機響應式設計 (新)
│   ├── responsive-camera.css   # 相機特定回應
│   └── ...
├── index.html                   # 首頁
├── highway.html                 # 國道監視器
├── expressway.html              # 快速道路
├── road.html                    # 省道
├── city.html                    # 市區道路
├── combined-roads.html          # 綜合道路
├── train-liveboard.html         # 台鐵看板
├── metro-liveboard.html         # 捷運看板
├── water-resources.html         # 水資源
├── air-quality.html             # 空氣品質
├── rainfall-radar.html          # 降雨雷達
├── weather.html                 # 氣象
├── contact.html                 # 聯絡我們
└── ...
```

---

## 🚀 最佳實踐

### DO ✅
- ✅ 使用 `<meta name="viewport">`
- ✅ 用 `rem` 或 `em` 單位
- ✅ 測試觸控交互
- ✅ 提供鍵盤支持
- ✅ 使用語義 HTML
- ✅ 優化圖片大小
- ✅ 最小化 CSS/JS
- ✅ 使用漸進式增強

### DON'T ❌
- ❌ 將字體設為絕對像素值
- ❌ 使用固定寬度佈局
- ❌ 依賴懸停交互
- ❌ 使用大型未優化圖片
- ❌ 禁用縮放
- ❌ 使用過時的 HTML
- ❌ 忽略無障礙性
- ❌ 假設網路速度

---

## 📞 反饋和改進

若發現任何手機相容性問題，請透過聯絡我們頁面回報：
- 📧 技術支援：tech@example.com
- 🔗 GitHub Issues
- 📱 線上客服

---

## 📚 參考資源

- [MDN: Responsive Design](https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design)
- [Google Mobile-Friendly Test](https://search.google.com/test/mobile-friendly)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [iOS Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/ios/)
- [Android Material Design](https://material.io/design)

---

**最後更新**: 2025年11月21日  
**版本**: 1.0  
**支持手機**: iOS 12+, Android 5+  
**支持瀏覽器**: Chrome, Firefox, Safari, Edge
