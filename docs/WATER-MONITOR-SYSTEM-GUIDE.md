# 水利署監視器影像系統 - 使用說明

## 📋 系統概述

**經濟部水利署即時監控影像系統**是一個完整的監視器管理和展示平台，專為水利署河川、水庫等水利設施的監控影像設計。

### 主要特色

✅ **即時統計面板** - 顯示總監視器數、直轄/合建比例、線上狀態
✅ **多維度篩選** - 依主管機關、影像狀態、名稱搜尋
✅ **智能排序** - 支援名稱、位置、狀態排序
✅ **響應式設計** - 完美適配桌面、平板、手機
✅ **現代化界面** - 美觀的漸變色卡片設計
✅ **即時時間** - 自動更新當前時間顯示

---

## 🚀 快速開始

### 1. 檔案結構

```
Road-Camera/
├── water-monitor.html              # 主頁面
└── assets/
    └── water-monitor-data.js       # 資料生成器和邏輯
```

### 2. 開啟系統

直接在瀏覽器中開啟 `water-monitor.html` 即可使用：

```bash
# Windows
start Road-Camera/water-monitor.html

# macOS/Linux
open Road-Camera/water-monitor.html
```

### 3. 系統需求

- 現代瀏覽器（Chrome 90+, Firefox 88+, Safari 14+, Edge 90+）
- 網路連線（用於載入 Font Awesome 圖標）
- 無需後端伺服器，純前端運行

---

## 📊 功能說明

### 統計面板

系統頂部顯示四個關鍵統計指標：

1. **總監視器數** 🎥
   - 顯示系統中所有監視器總數
   - 包含線上和離線設備

2. **水利署直轄** 🏢
   - 水利署直接管理的監視器數量
   - 紫色標識

3. **地方合建** 🏙️
   - 與地方政府合作建置的監視器
   - 藍色標識

4. **有影像監視器** 📡
   - 當前正常運作中的監視器數量
   - 即時顯示更新時間

### 篩選功能

#### 主管機關篩選
- **全部機關** - 顯示所有監視器
- **水利署直轄** - 僅顯示水利署直管設備
- **地方合建** - 僅顯示地方合建設備

#### 影像狀態篩選
- **全部監視器** - 顯示所有設備
- **有影像** - 僅顯示正常運作的監視器
- **無影像** - 僅顯示離線或故障的監視器

#### 排序方式
- **依名稱排序** - 按監視器名稱字母順序
- **依位置排序** - 按地理位置排序
- **依狀態排序** - 線上設備優先顯示

#### 搜尋功能
- 支援監視器名稱搜尋
- 支援地理位置搜尋
- 支援河川名稱搜尋
- 即時過濾，無需按Enter

### 監視器卡片

每張卡片包含完整資訊：

```
┌─────────────────────────┐
│   [監視器影像區域]       │ ← 250px 高度影像
│   ● 線上/離線狀態        │
└─────────────────────────┘
│ 🎥 監視器名稱           │
│ 📍 地理位置             │
│ 🌊 所屬河川             │
│ 💧 水位資訊 (如有)      │
│ 📊 流量資訊 (如有)      │
│ #️⃣ 設備編號             │
│ [直轄/合建 徽章]        │
└─────────────────────────┘
```

#### 卡片互動效果
- **懸停效果** - 卡片上浮、陰影增強
- **影像縮放** - 影像輕微放大效果
- **狀態指示** - 閃爍動畫顯示即時狀態

---

## 🎨 設計規範

### 色彩系統

#### 主題色
- **主背景漸變** - `#667eea` → `#764ba2`
- **卡片背景** - `#FFFFFF`
- **主文字** - `#2c3e50`
- **次要文字** - `#7f8c8d`

#### 狀態色
- **線上狀態** - `rgba(76, 175, 80, 0.9)` 綠色
- **離線狀態** - `rgba(244, 67, 54, 0.9)` 紅色

#### 統計圖標漸變
- **總數** - 紫藍漸變 `#667eea` → `#764ba2`
- **直轄** - 粉紅漸變 `#f093fb` → `#f5576c`
- **合建** - 藍青漸變 `#4facfe` → `#00f2fe`
- **線上** - 綠青漸變 `#43e97b` → `#38f9d7`

### 響應式斷點

```css
/* 手機版 */
@media (max-width: 768px) {
    - 單欄顯示
    - 統計卡片堆疊
    - 控制項垂直排列
}

/* 平板/桌面 */
@media (min-width: 769px) {
    - 多欄網格布局
    - 自適應欄數 (350px 最小寬度)
}
```

---

## 🔧 技術實作

### 核心技術
- **HTML5** - 語義化標籤
- **CSS3** - Flexbox + Grid + 動畫
- **Vanilla JavaScript** - 無框架依賴
- **Font Awesome 6.4** - 圖標庫

### 資料結構

```javascript
{
    id: 'WRA-001-1',              // 設備編號
    name: '淡水河監視站 #1',      // 監視器名稱
    location: '新北市淡水區',     // 地理位置
    river: '淡水河',              // 所屬河川
    type: 'direct',               // direct/local
    status: 'online',             // online/offline
    latitude: 25.0330,            // 緯度
    longitude: 121.5654,          // 經度
    altitude: 50,                 // 海拔高度
    imageUrl: 'https://...',      // 影像URL
    lastUpdate: '2025-11-06...',  // 最後更新時間
    waterLevel: '2.35',           // 水位(公尺)
    flow: 450                     // 流量(CMS)
}
```

### 主要函式

#### `generateDemoData()`
生成示範資料，包含全台26個主要河川的監視器

#### `updateStats()`
更新統計面板的數字顯示

#### `updateTime()`
更新當前時間顯示（每分鐘）

#### `filterCameras()`
根據篩選條件過濾監視器列表

#### `renderCameras()`
渲染監視器卡片到頁面

#### `createCameraCard(camera)`
創建單個監視器卡片HTML

---

## 🔄 整合真實 API

### 步驟 1: 取得 API 權限

聯繫**經濟部水利署**申請 API 存取權限：
- 📧 Email: service@wra.gov.tw
- 🌐 官網: https://www.wra.gov.tw/
- 📞 客服: 02-37073000

### 步驟 2: 修改資料來源

編輯 `assets/water-monitor-data.js`：

```javascript
// 原始（示範資料）
function loadCameraData() {
    setTimeout(() => {
        allCameras = generateDemoData();
        filteredCameras = [...allCameras];
        updateStats();
        renderCameras();
    }, 1000);
}

// 修改為真實 API
async function loadCameraData() {
    try {
        const response = await fetch('YOUR_API_ENDPOINT', {
            headers: {
                'Authorization': 'Bearer YOUR_API_KEY'
            }
        });
        const data = await response.json();
        
        allCameras = transformApiData(data);
        filteredCameras = [...allCameras];
        updateStats();
        renderCameras();
    } catch (error) {
        console.error('載入資料失敗:', error);
        showErrorMessage();
    }
}

// API 資料轉換函式
function transformApiData(apiData) {
    return apiData.cameras.map(cam => ({
        id: cam.stationId,
        name: cam.stationName,
        location: cam.city + cam.district,
        river: cam.riverName,
        type: cam.managementType,
        status: cam.isOnline ? 'online' : 'offline',
        latitude: cam.lat,
        longitude: cam.lng,
        altitude: cam.elevation,
        imageUrl: cam.imageUrl,
        lastUpdate: cam.updateTime,
        waterLevel: cam.waterLevel,
        flow: cam.flowRate
    }));
}
```

### 步驟 3: 啟用自動更新

```javascript
// 每5分鐘更新一次資料
setInterval(loadCameraData, 5 * 60 * 1000);

// 每30秒更新一次影像
setInterval(refreshImages, 30 * 1000);
```

---

## 📱 行動裝置最佳化

### 觸控優化
- 增大可點擊區域（44px+）
- 支援手勢操作
- 快速響應觸控

### 效能優化
- 圖片延遲載入（lazy loading）
- 減少DOM操作
- 使用CSS硬體加速

### 網路優化
- 圖片壓縮
- CDN加速
- 離線快取支援

---

## 🐛 常見問題

### Q1: 為什麼看不到影像？

**A:** 
1. 檢查 `assets/water-monitor-data.js` 是否正確載入
2. 開啟瀏覽器開發者工具查看 Console 錯誤
3. 確認網路連線正常
4. 檢查圖片URL是否有效

### Q2: 統計數字不正確？

**A:**
1. 確認 `updateStats()` 函式被正確調用
2. 檢查篩選邏輯是否影響統計
3. 查看資料結構是否符合預期

### Q3: 篩選功能不工作？

**A:**
1. 檢查事件監聽器是否正確綁定
2. 確認 `filterCameras()` 函式邏輯
3. 查看 Console 是否有JavaScript錯誤

### Q4: 手機版顯示異常？

**A:**
1. 確認 viewport meta tag 正確設置
2. 檢查 CSS media queries
3. 測試不同螢幕尺寸

### Q5: 如何自訂樣式？

**A:**
直接修改 `<style>` 標籤內的CSS，或創建外部樣式表：

```html
<link rel="stylesheet" href="assets/water-monitor-custom.css">
```

---

##
