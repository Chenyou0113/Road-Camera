# 專案統一配色方案參考卡
**版本**: 1.0  
**更新**: 2025年11月1日

---

## 🎨 核心色系

### 主色調
```
深藍色 (Primary Blue):  #1e40af  →  rgb(30, 64, 175)
青色 (Cyan):            #0891b2  →  rgb(8, 145, 178)
亮青色 (Light Cyan):    #06b6d4  →  rgb(6, 182, 212)
```

### CSS 複製代碼

#### 背景漸層 (推薦用於body)
```css
background: linear-gradient(135deg, #1e40af 0%, #0891b2 100%);
```

#### 按鈕漸層
```css
background: linear-gradient(135deg, #1e40af, #0891b2);
```

#### 按鈕懸停效果
```css
background: linear-gradient(135deg, #0891b2, #06b6d4);
transition: all 0.3s ease;
```

#### 文字顏色 (標題、標籤)
```css
color: #1e40af;  /* 主標題 */
color: #0891b2;  /* 副標題或強調 */
```

#### 邊框和分割線
```css
border: 2px solid #1e40af;
border-bottom: 2px solid #0891b2;
```

---

## 📝 使用指南

### ✅ 正確使用
- 背景: 使用漸層以避免單調
- 標題: 使用 #1e40af (深藍)
- 強調: 使用 #0891b2 (青色)
- 邊框: 使用 #1e40af
- 懸停: 轉換到 #0891b2 或 #06b6d4

### ❌ 避免使用
- 不再使用的舊色: #667eea, #764ba2, #f093fb, #FF9800, #2196F3, #9C27B0
- 避免混用新舊顏色
- 不使用完全不相關的色系

---

## 🎯 快速查表

| 元素 | 顏色代碼 | 用途 |
|------|---------|------|
| 頁面背景 | `#1e40af → #0891b2` | 漸層背景 |
| 標題 H1/H2 | `#1e40af` | 主要標題 |
| 標題 H3/標籤 | `#0891b2` | 副標題/標籤 |
| 主按鈕 | `#1e40af` | 主要按鈕 |
| 按鈕懸停 | `#0891b2` | 交互反饋 |
| 邊框/分割線 | `#1e40af` | 視覺分隔 |
| 圖標強調 | `#0891b2` | 圖標顏色 |
| 成功狀態 | `#06b6d4` | 確認/成功 |

---

## 💾 複製快速代碼片段

### React/Vue
```vue
<style>
  .primary { color: #1e40af; }
  .accent { color: #0891b2; }
  .button { background: linear-gradient(135deg, #1e40af, #0891b2); }
  .button:hover { background: linear-gradient(135deg, #0891b2, #06b6d4); }
</style>
```

### Tailwind CSS
```html
<!-- 如果使用自訂配置 -->
<script>
  // tailwind.config.js
  module.exports = {
    theme: {
      colors: {
        primary: '#1e40af',
        accent: '#0891b2',
        light: '#06b6d4'
      }
    }
  }
</script>
```

### 原生 CSS
```css
:root {
  --color-primary: #1e40af;
  --color-accent: #0891b2;
  --color-light: #06b6d4;
  --gradient-primary: linear-gradient(135deg, #1e40af 0%, #0891b2 100%);
}

body { background: var(--gradient-primary); }
h1 { color: var(--color-primary); }
button { background: var(--gradient-primary); }
```

---

## 🔄 顏色過渡方案 (若需要漸進式更新)

### 第一階段: 主色系
```
新色: #1e40af (深藍)
舊色: #667eea, #FF9800, #2196F3, #9C27B0
```

### 第二階段: 強調色
```
新色: #0891b2 (青色)
舊色: #764ba2, #F57C00, #1976D2, #4CAF50
```

### 第三階段: 補充色
```
新色: #06b6d4 (亮青)
舊色: #f093fb, #21CBF3
```

---

## 📱 響應式考慮

色配在所有尺寸中保持一致:
- ✅ 桌面 (1920px+)
- ✅ 平板 (768px - 1024px)
- ✅ 手機 (< 768px)
- ✅ 深色模式 (未來計劃)

---

## 🧪 測試清單

開發新功能時，請確認:
- [ ] 背景色使用統一漸層
- [ ] 所有按鈕採用新配色
- [ ] 標題和標籤使用正確色碼
- [ ] 邊框和分割線一致
- [ ] 懸停效果平順過渡
- [ ] 移動端顯示無誤

---

## 📞 問題排查

**Q: 我應該何時使用 #1e40af 而不是 #0891b2?**  
A: #1e40af (深藍) 用於主要元素如標題和主按鈕，#0891b2 (青色) 用於強調、次要元素和懸停效果。

**Q: 舊色代碼是否還會使用?**  
A: 不應該。所有新代碼應使用新的統一配色。舊色代碼只在保留的舊頁面中可能存在。

**Q: 深色模式如何應用?**  
A: 深色模式正在規劃中，色碼會根據背景自動調整。暫時請保持當前配色。

---

**記住**: 統一的色系提升品牌形象！ 🎨✨
